<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DeKoelkast.Pages.BillPage"
             Title="Rekening">
    <StackLayout Padding="20">
        <Label Text="Je moet (bedrag van rekening)€ betalen tot vrijdagavond. Anders mag je niet meer drinken."
               FontSize="18"
               HorizontalOptions="Center"
               Margin="0,20,0,20" />
        <Button Text="Uitstellen"
                HorizontalOptions="FillAndExpand"
                WidthRequest="250"
                Margin="0,10,0,0"
                Clicked="OnUitstellenButtonClicked" />
        <Button Text="OK"
                HorizontalOptions="FillAndExpand"
                WidthRequest="250"
                Margin="0,10,0,0"
                Clicked="OnOkButtonClicked" />
    </StackLayout>
</ContentPage>